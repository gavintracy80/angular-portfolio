import { PhotoDirective } from './photo.directive';

describe('HighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new PhotoDirective();
    expect(directive).toBeTruthy();
  });
});
